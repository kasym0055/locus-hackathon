# Tavily Integration Implementation Plan

> Execute inline using superpowers:executing-plans and test-driven-development.

**Goal:** Connect bounded Tavily discovery, publisher parsing and existing OpenAI assessment.
**Architecture:** One selected search adapter, shared atomic accounting, request-local source-bound discovery hints; publisher evidence remains authoritative.
**Tech Stack:** TypeScript, Next.js, Zod, existing Redis Lua ledger, Vitest, OpenAI SDK.
**Spec:** docs/superpowers/specs/2026-09-17-tavily-design.md

## Global constraints

27-second profile deadline; 3-second discovery timeout; 12 HTML attempts with licensed-file reservation unchanged; no publisher/security/verification weakening; no Task 6. Exclude pre-existing safe-fetch changes. Every commit includes the required co-author trailer.

## Execution

- [x] Add failing integration tests in tests/integration/tavily.test.ts for fixed POST, normalization, page/image bindings, four-call cap, failed dispatch accounting and cancellation. Add ledger argument tests for a separate Tavily pool, default zero, request cap four.
- [x] Extend providerJson with an optional POST body while retaining transport timeout, size limit and redirects:error. Implement createTavilySearch in discovery/tavily.ts and shared types in discovery/search.ts. Requests set search_depth=basic, max_results=10, safe_search=true, include_answer=false, include_raw_content=false, auto_parameters=false and include_usage=true. Settle known credits, retain unknown reservation on failures.
- [x] Add optional typed snippet on DiscoveryRecord. Add discovery/context.ts collector with request-local WeakMap; retain at most 40 snippets of 600 characters, match SHA256 canonical page URL to approved evidence IDs, deduplicate and cap model context at 16 records. Red tests for cross-request isolation and unmatched sources.
- [x] Test and implement separate validated AssessmentInput.discoveryContext; require matching image/evidence pairs, redact URLs, cap text, label it non-authoritative in OpenAI instructions. Send actual parser corroboration excerpts along with primary evidence; do not change deterministic scoring.
- [x] Wire provider selection in profile/services.ts and config.ts; default brave, no automatic fallback; add separate Tavily ledger reserve kind and verified allowance zero. Preserve Brave/AI accounting. Document env and normalization.
- [x] Run focused tests, full suite, typecheck, lint, production build, git diff --check. Review staged diff and exclude secrets and pre-existing edits. Commit and push feat/m1-vertical-slice. Stop for env configuration / Production promotion.

Production environment configuration, promotion and live acceptance remain operator-gated; no M1 closure claimed.
